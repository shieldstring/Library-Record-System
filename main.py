from book_list import BookList
from user_list import UserList
from loan import Loan

def main():
    # Instantiate the system components
    book_list = BookList()
    user_list = UserList()
    loan_system = Loan()
    
    # Add books, users, and manage loans
    # Example usage:
    # book_list.add_book(...)
    # user_list.add_user(...)
    # loan_system.borrow_book(...)
    
if __name__ == "__main__":
    main()
